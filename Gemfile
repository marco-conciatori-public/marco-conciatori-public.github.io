# frozen_string_literal: true

source "https://rubygems.org"

# gem "rails"

# gem "jekyll", "~> 4.4"
# We use the modern github-pages gem, but restrict it to a newer version range
gem "github-pages", "~> 228", group: :jekyll_plugins

# Modern Ruby versions require the webrick gem explicitly for Jekyll to run locally
gem "webrick", group: :jekyll_plugins