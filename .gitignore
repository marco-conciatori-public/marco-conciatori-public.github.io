.idea/
.claude/

CLAUDE.md

# Jekyll build output (only created if you build the site locally)
_site/
.jekyll-cache/
.sass-cache/

# Node / Tailwind: dependencies and the generated CSS (rebuilt by CI on every push)
node_modules/
assets/css/tailwind.css